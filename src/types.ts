type TSwitchDirection = 'next' | 'previous'
type TSlidePosition = 'current' | TSwitchDirection
type TSlidePositions = [TSlidePosition, TSlidePosition, TSlidePosition]

export type { TSlidePosition, TSlidePositions, TSwitchDirection }
